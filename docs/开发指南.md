# 开发指南

## 🛠️ 环境搭建

### 前置要求
1. **Rust**: 安装最新稳定版。
2. **Node.js**: 建议使用 LTS 版本。
3. **pnpm**: 推荐的包管理工具。

### 初始化步骤
```bash
git clone <repository-url>
cd tauri-acgn
pnpm install
```

## 💻 本地开发

运行开发服务器：
```bash
pnpm tauri dev
```

## 📂 项目结构

- `src/`: 前端代码 (Vanilla JS, CSS, HTML)。
- `src-tauri/`: Rust 后端代码。
- `docs/`: 项目文档。
- `assets/`: 静态资源。

## 📏 编码规范

- **Git 提交**: 遵循 `feat:`, `fix:`, `docs:`, `style:`, `refactor:` 前缀。
- **Rust**: 使用 `cargo fmt` 格式化代码。
- **前端**: 使用标准 ESM 模块化。
